const theme = {
  colors: {
    charcoalBlack: '#333333', // Add this color if missing
    ivory: '#FFFFF0',          // Add this color if missing
    sageGreen: '#B2AC88',      // Add this color if missing
    pearlWhite: '#F5F5F5',     // Add this color if missing
    outerSpace: '#2D383A',     // Add any other missing colors
    copper: '#B87333',         // Ensure all other referenced colors are here
  },
};

export default theme;
